#include <bits/stdc++.h>
using namespace std;

#define forr(i,n) for(int i = 0; i < n; i++)
#define ll long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m ,n ;
    cin >> m >> n;
    cout << (m*n)/2;

    return 0;
}